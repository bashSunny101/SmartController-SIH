# Server configuration
PORT=3000
DB_FILE=./server/data.sqlite
JWT_SECRET=change_me_to_a_secure_random_value
ALLOW_DEMO_OTP=1

# SMTP (optional, used to email OTPs)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=0
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
